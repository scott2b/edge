# source me to source .env. Alternatively, you might consider managing your enviroment with:
# `autenv`: https://github.com/kennethreitz/autoenv
# or `direnv` (with .envrc instead of .env):  https://direnv.net/
set -a
. .env
set +a
